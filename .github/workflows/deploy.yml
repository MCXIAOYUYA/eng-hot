name: Deploy to Pages

# 触发时机:
#  - 推代码到 main(手动改动)
#  - "Refresh data" 每日刷新完成后(数据更新自动重新部署)
#  - Actions 页手动触发
on:
  push:
    branches: [main]
  workflow_run:
    workflows: ["Refresh data"]
    types: [completed]
  workflow_dispatch: {}

# GitHub Pages 部署所需权限
permissions:
  contents: read
  pages: write
  id-token: write

# 同一时刻只保留最新一次部署
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    # 若是 Refresh data 触发,仅在其成功时才部署
    if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: web/package.json

      - name: Install & build
        working-directory: web
        # 仓库不提交 package-lock.json:Windows 生成的锁文件只含 win32 原生
        # 二进制,会让 Linux runner 缺 @rollup/rollup-linux-x64-gnu 而构建失败。
        # 这里用 npm install 现场按 runner 平台解析依赖,稳妥跨平台。
        run: |
          npm install --no-audit --no-fund
          npm run build

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: web/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
